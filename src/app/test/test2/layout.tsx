export const metadata = {
  title: 'react-video test2',
  description: 'react-video test2',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return (
    <>{children}</>
  );
}
