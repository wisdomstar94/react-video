export const metadata = {
  title: 'react-video test5',
  description: 'react-video test5',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return (
    <>{children}</>
  );
}
