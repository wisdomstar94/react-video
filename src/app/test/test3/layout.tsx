export const metadata = {
  title: 'react-video test3',
  description: 'react-video test3',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return (
    <>{children}</>
  );
}
