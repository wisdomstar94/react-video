export const metadata = {
  title: 'react-video test1',
  description: 'react-video test1',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return (
    <>{children}</>
  );
}
